from handlers.file_handler import generate_data_hash
from handlers.log_handler import log
from time import sleep

# Main function
if __name__ == '__main__':
    # data_hash = generate_data_hash()
    # log("Data hash generated")
    # log("Data hash: %s" % data_hash)

    # log("=== Database manager has been run ===")
    print("Program execution")
    sleep(3600 * 1000)